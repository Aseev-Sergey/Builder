package sample.schemes.nodes;

import javafx.scene.shape.Circle;

public class Begin extends Circle {

    public Begin(double centerX, double centerY, double radius){
        super(centerX, centerY, radius);
    }
}
